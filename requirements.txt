# Core stack (see gainz2/settings.py — Django 6 + Channels)
Django>=6.0,<6.1
channels>=4.2,<5
asgiref>=3.8
daphne>=4.2,<5
psycopg[binary]>=3.1
python-dotenv>=1.0,<2.0

# Optional later (AGENTS.md): PostgreSQL, Redis, workers — uncomment when wired in settings
# redis>=5.0
# django-rq>=2.10
